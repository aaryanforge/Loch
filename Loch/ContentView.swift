//
//  ContentView.swift
//  Loch
//
//  Created by A P on 30/1/2024.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Test!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
